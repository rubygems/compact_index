require 'tempfile'
require 'spec_helper'
require 'compact_index/versions_file'
require 'support/versions_file'

describe CompactIndex::VersionsFile do
  describe "#create"  do
    pending "create the versions.list file with given gems"
  end

  describe "#update" do
    pending "updte the versions.list file with given gems"
  end

  describe "#get" do
    pending "recover the versions file"
    context "with extra gems" do
      pending "add the gems temporarely on get output"
    end
  end
end
